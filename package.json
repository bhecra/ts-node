{
  "name": "multi-tenant",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "tsc": "tsc",
    "dev": "nodemon --config nodemon.json ./src/app/app.ts",
    "dev:debug": "nodemon --config nodemon.json --inspect ./src/app/app.ts",
    "start": "ts-node-dev --inspect --respawn --transpile-only ./src/app/app.ts",
    "prod": "tsc && node ./build/app.js ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.11",
    "ts-node-dev": "^1.1.1",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
